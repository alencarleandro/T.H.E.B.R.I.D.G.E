{
  "name": "the-bridge",
  "version": "0.1.0",
  "description": "Streaming pessoal de jogos para Windows e Android",
  "main": "desktop/main.cjs",
  "author": "T.H.E.B.R.I.D.G.E",
  "private": true,
  "scripts": {
    "start": "electron .",
    "native": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-input.ps1",
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "node tests/e2e.cjs",
    "build:win": "npm run native && electron-builder --win portable --config build.config.cjs",
    "android:sync": "cap sync android",
    "build:android": "npm run android:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.9",
    "@capacitor/core": "^7.6.9",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.9",
    "@playwright/test": "^1.63.0",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3"
  }
}
